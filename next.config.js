/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Required for Capacitor static export
  // Comment this out if you want to keep server-side features
  // output: 'export',
}

module.exports = nextConfig
